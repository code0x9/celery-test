[project]
name = "celery-test"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "celery>=5.5.3",
    "redis>=7.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
